{
  "name": "control-work-to-diplom",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "json-server -p 3001 -w db.json"

  },
  "devDependencies": {
    "vite": "^6.0.1"
  }
}
